{
  "name": "merge-queues",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "scripts": {
    "start": "npx serve -s public -l 3000",
    "build": "echo 'No build step for basic setup'"
  }
}

